export default class Flow {
  constructor(name, frequency, url, actions, viewport, id) {
    this.name = name;
    this.frequency = frequency;
    this.url = url;
    this.actions = actions;
    this.viewport = viewport;
    this.id = id;
  }
}
